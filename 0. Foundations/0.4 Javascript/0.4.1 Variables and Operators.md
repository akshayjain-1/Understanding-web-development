- In the previous sections you learned how to structure webpages with HTML and style them with CSS. The next step is to make the webpage interactive, which is exactly what JavaScript is for.
- The simplest way to get started is to create an HTML file with the JavaScript code inside of it.
- By default we execute JS code in browser

# Hello World!
- As we all know the custom, we start with the most infamous 'Hello World!' program
```
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>

  <script>
    // Your JavaScript goes here!
    console.log("Hello, World!")
  </script>

</body>
</html>
```
- Save and open this file up in a web browser and then open up the browser’s console:
    - Right-click on the blank webpage.
    - Click on “Inspect” or “Inspect Element” to open the Developer Tools.
    - Find and select the “Console” tab, where you should see the output of our console.log statement. 

- `console.log()` is the command to print something to the developer console in your browser.
- Another way to include JavaScript in a webpage is through an external script. This is very similar to linking external CSS docs to your website.
    - `<script src="javascript.js"></script>`
- JavaScript files have the extension `.js` and filename can be anything.

# Variables
- These are the building blocks of any program. You can think of variables as “storage containers” for data in your code.
- You can declare variables using the `let` keyword.
```
let firstName = "John";
let lastName = "Doe";

console.log(firstName);
console.log(lastName);
```
- You can also reassign variables
```
let age = 11;
console.log(age); // outputs 11 to the console

age = 54;

console.log(age); // what will be output now?
```
- `let` is used to just declare new variables being introduced. Once it has been declared, we can skip using `let` to make any modifications to the code (as observed in the above snippet with age = 54;)
- If we do not want a variable to change its value throughout the codebase, We can accomplish this using the `const` keyword.
```
const pi = 3.14;
pi = 10; // this will lead to an error, before even reaching the console.log() because we cannot reassign const variables

console.log(pi);
```
- So in summary, there are two ways to declare a variable:
    - `let`, which we can re-assign.
    - `const` which we can’t re-assign and will throw an error if we try.

- There is also a third way, `var`, which was the original way variables were declared in JavaScript. `var` is similar to `let` in that variables assigned this way can be reassigned. 
    - By and large, it is not used anymore. However, you will likely come across code which uses `var` at some point.

# Numbers
- JavaScript follows the standard mathematical **Order of Operations** (often remembered by acronyms like **PEMDAS** or **BODMAS**)
    - Parentheses (or Brackets) are evaluated first.
    - Exponentiation is done next, from right to left.
    - Multiplication and Division are done next, from left to right.
    - Addition and Subtraction are done last, from left to right.

## References
- [MDN’s “What is JavaScript?”](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/First_steps/What_is_JavaScript)
- [variables in JavaScript](https://javascript.info/variables)
- [JavaScript math](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/First_steps/Math)
- [JavaScript operators](http://javascript.info/operators)